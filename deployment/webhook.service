[Unit]
Description=Webhook Listener
After=network.target

[Service]
ExecStart=/usr/bin/webhook -hooks /home/ubuntu/deployment/hooks.json -port 9000
WorkingDirectory=/home/ubuntu/deployment
User=ubuntu
Restart=always

[Install]
WantedBy=multi-user.target
